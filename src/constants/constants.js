const responseMessageType = {
    channel: 'channel',
    direct: 'direct',
    reply: 'reply'
}

const responseActionType = {
    message: 'message',
    action: 'action'
}

module.exports = {
    responseMessageType,
    responseActionType
}