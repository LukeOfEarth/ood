const responseMessageType = {
    channel: 'channel',
    direct: 'direct',
    reply: 'reply'
}

const responseActionType = {
    message: 'message',
}

module.exports = {
    responseMessageType,
    responseActionType
}